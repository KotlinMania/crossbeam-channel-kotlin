# Port targets — `crossbeam-channel-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `crossbeam-channel` that codex-rs uses, and therefore the minimum subset that `crossbeam-channel-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-36] (rank `175`, tier `direct`, status `To Do`)

**Cargo package name**: `crossbeam-channel`
**Rust module name (in `use` statements)**: `crossbeam_channel`

## Summary

- **6** distinct symbols imported by codex-rs
- **6** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `crossbeam_channel::Receiver`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:1` (`codex-file-search`) — `use crossbeam_channel::Receiver`

### `crossbeam_channel::Sender`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:2` (`codex-file-search`) — `use crossbeam_channel::Sender`

### `crossbeam_channel::after`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:3` (`codex-file-search`) — `use crossbeam_channel::after`

### `crossbeam_channel::never`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:4` (`codex-file-search`) — `use crossbeam_channel::never`

### `crossbeam_channel::select`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:5` (`codex-file-search`) — `use crossbeam_channel::select`

### `crossbeam_channel::unbounded`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:6` (`codex-file-search`) — `use crossbeam_channel::unbounded`
