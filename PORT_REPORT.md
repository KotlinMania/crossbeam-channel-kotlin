# Port report — `crossbeam-channel-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `crossbeam-channel`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 13 `use` sites from 3 other repos
- **Outbound**: 6 `use` sites to 1 other repos
- **Unresolved external**: 2 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `crossbeam-channel` | `crossbeam_channel` | 13 | 1 | COD-36 | 175 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (13 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-file-search` | 6 | ✅ |
| `tracing-appender-kotlin` | `tracing-appender` | 6 |  |
| `webbrowser-kotlin` | `webbrowser` | 1 |  |

### Symbol-level demand (11 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `crossbeam_channel::Receiver` | 2 | 1 | 1 | 2 |
| `crossbeam_channel::Sender` | 2 | 1 | 1 | 2 |
| `crossbeam_channel::after` | 1 | 1 | 0 | 1 |
| `crossbeam_channel::never` | 1 | 1 | 0 | 1 |
| `crossbeam_channel::select` | 1 | 1 | 0 | 1 |
| `crossbeam_channel::unbounded` | 1 | 1 | 0 | 1 |
| `crossbeam_channel` | 1 | 0 | 1 | 1 |
| `crossbeam_channel::RecvError` | 1 | 0 | 1 | 1 |
| `crossbeam_channel::SendTimeoutError` | 1 | 0 | 1 | 1 |
| `crossbeam_channel::TryRecvError` | 1 | 0 | 1 | 1 |
| `crossbeam_channel::bounded` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 11 symbols)

##### `crossbeam_channel::Receiver` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:1` — `use crossbeam_channel::Receiver`

Other workspace call sites:

- `tracing-appender-kotlin/tracing-appender` `src/worker.rs:2` — `use crossbeam_channel::Receiver`

##### `crossbeam_channel::Sender` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:2` — `use crossbeam_channel::Sender`

Other workspace call sites:

- `tracing-appender-kotlin/tracing-appender` `src/non_blocking.rs:49` — `use crossbeam_channel::Sender`

##### `crossbeam_channel::after` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:3` — `use crossbeam_channel::after`

##### `crossbeam_channel::never` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:4` — `use crossbeam_channel::never`

##### `crossbeam_channel::select` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:5` — `use crossbeam_channel::select`

##### `crossbeam_channel::unbounded` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:6` — `use crossbeam_channel::unbounded`

##### `crossbeam_channel` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `tests/common.rs:3` — `use crossbeam_channel as cbc`

##### `crossbeam_channel::RecvError` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-appender-kotlin/tracing-appender` `src/worker.rs:2` — `use crossbeam_channel::RecvError`

##### `crossbeam_channel::SendTimeoutError` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-appender-kotlin/tracing-appender` `src/non_blocking.rs:49` — `use crossbeam_channel::SendTimeoutError`

##### `crossbeam_channel::TryRecvError` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-appender-kotlin/tracing-appender` `src/worker.rs:2` — `use crossbeam_channel::TryRecvError`

##### `crossbeam_channel::bounded` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-appender-kotlin/tracing-appender` `src/non_blocking.rs:49` — `use crossbeam_channel::bounded`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `rand-kotlin` | 6 | 2 | `rand` | COD-114 | 14 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `crossbeam-utils` | 25 | 5 | `crossbeam-channel` |
| `signal-hook` | 3 | 3 | `crossbeam-channel` |
